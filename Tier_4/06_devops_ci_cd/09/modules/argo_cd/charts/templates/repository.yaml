{{- range .Values.applications }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: argocd
  labels:
    argosd.argoproj.io/secret-type: repository
stringData:
  url: {{ .url }}
  username: {{ .username }}
  password: {{ .password }}
  insecure: "{{ $.Values.repoConfig.insecure }}"
  enableLfs: "{{ $.Values.repoConfig.enableLfs }}"
---
{{- end }}
